Simple search service hosted in AWS EC2 using Flask (server) and Whoosh (search engine). \
Replaced OpenSearch with this to cut costs.

Code reads gamenames from RDS and Whoosh creates an index directory for them.
